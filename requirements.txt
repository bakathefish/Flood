earthengine-api>=1.4
pandas>=2.2,<3
pytest>=8.0
xgboost>=2.1
scikit-learn>=1.5
shap>=0.46
matplotlib>=3.9
numpy>=1.26
scipy>=1.11
pillow>=10
joblib>=1.3
rasterio>=1.3
# login-free satellite path (Planetary Computer + WMS)
pystac-client>=0.9
planetary-computer>=1.0
rioxarray>=0.22
pyproj>=3.7
shapely>=2.0
requests>=2.31
# data-fetch extras (pipeline/fetch_rain.py only): pip install imdlib xarray
# Reads the shipped PDFs so tests/test_pdf_parity.py can gate them. This was a
# comment rather than a dependency, so the whole PDF gate skipped silently on
# both runners: 18 assertions locally, 1 skip in CI, which is exactly the hole
# the gate was written to close. Also used by pipeline/make_ndem_panel.py.
pypdfium2>=4.30
# Read by tests/test_ci_wiring.py so the workflow's own wiring is gated. Listed
# as a real dependency rather than guarded with importorskip: the pypdfium2 line
# above is here because a "comment dependency" made a whole gate skip silently.
PyYAML>=6.0
